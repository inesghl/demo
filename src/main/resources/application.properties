# Application Name
spring.application.name=backend

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/TPWEB
spring.datasource.username=root
spring.datasource.password=bn!az$$9*_o
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=strongkeylongenoughtobesecretkey
#jwt.secret=${JWT_SECRET:defaultSecretKey}
jwt.expiration=86400000

# Debug logging for Spring Security
logging.level.org.springframework.security=DEBUG
logging.level.com.example.backend.security=DEBUG